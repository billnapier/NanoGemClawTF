# Quickstart - Telegram Messaging Gateway Verification

## Verification Steps
1. Run local telegram bot unit test suite:
   ```bash
   python3 -m unittest discover -s tests -p "test_telegram*.py"
   ```
2. Verify `/start` and `/status` command execution handlers.
